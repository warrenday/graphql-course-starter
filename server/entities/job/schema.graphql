enum JobType {
  FULL_TIME
  PART_TIME
  INTERNSHIP
}

type Job {
  id: ID!
  title: String!
  company: Company!
  location: String! @deprecated(reason: "Use officeAddress instead")
  description: String!
  type: JobType!
  remote: Boolean!
  salary: Int!
  createdAt: DateTime!
  updatedAt: DateTime!
  isApplied: Boolean!
  officeAddress: Address
}

input SearchJobsInput {
  query: String!
}

input CreateJobInput {
  title: String!
  location: String!
  description: String!
  type: JobType!
  remote: Boolean!
  salary: Int!
  companyName: String!
}

input DeleteJobInput {
  id: ID!
}

input ApplyForJobInput {
  id: ID!
}

input CancelApplicationInput {
  id: ID!
}

type Subscription {
  jobCreated: Job!
}

type Query {
  searchJobs(input: SearchJobsInput!): [Job!]!
}

type Mutation {
  createJob(input: CreateJobInput!): Job! @auth(role: ADMIN)
  deleteJob(input: DeleteJobInput!): Boolean!
  applyForJob(input: ApplyForJobInput!): Boolean!
  cancelApplication(input: CancelApplicationInput!): Boolean!
}
